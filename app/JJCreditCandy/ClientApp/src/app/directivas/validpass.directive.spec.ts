import { ValidpassDirective } from './validpass.directive';

describe('ValidpassDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidpassDirective();
    expect(directive).toBeTruthy();
  });
});
